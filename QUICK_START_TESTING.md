# Quick Start: DJ EQ Automation Testing\n\n## Test the Implementation Immediately\n\n### 1. Basic A/B Test (Quick Mix)\n\n```bash\n# Generate mix WITH EQ (default)\nmake quick-mix SEED='Deine Angst' TRACK_COUNT=3\n\n# Generate SAME mix WITHOUT EQ (baseline)\nmake quick-mix SEED='Deine Angst' TRACK_COUNT=3 EQ=off\n\n# Compare the two output files\n```\n\n### 2. Full Render Test\n\n```bash\n# Render with EQ\nmake render\n\n# Render without EQ\nmake render EQ=off\n```\n\n### 3. Run Unit Tests\n\n```bash\ncd /home/mcauchy/autodj-headless\npython3 test_eq_automation.py\n```\n\nExpected output:\n```\n✓ Engine initialization\n✓ Opportunity detection (realistic features)\n✓ Low-confidence rejection\n✓ Timeline generation\n✓ Sample/bar conversion\n✓ Detector facade\n✓ Envelope definitions\n\n✅ ALL TESTS PASSED\n```\n\n---\n\n## What to Listen For\n\n### With EQ (Default)\n- **Smoother energy flow** between sections\n- **Cleaner transitions** (less frequency clash)\n- **Tension building** effects (e.g., bass dips before drops)\n- **More professional sound** overall\n\n### Without EQ (Baseline)\n- **Harsher transitions** (if frequencies clash)\n- **Constant energy** (no dynamic shaping)\n- **Raw mix** (as-is from sequencing)\n\n---\n\n## Where Output Files Are Saved\n\n- **Quick mixes:** Check `/home/mcauchy/autodj-headless/` for latest `.mp3` files\n- **Full renders:** Check `data/mixes/` directory\n- **Logs:** Watch console output for EQ status\n\n---\n\n## Verifying EQ is Working\n\n### In Logs\n\nLook for:\n```\nDJ EQ Automation: ENABLED (per-track EQ automation)\nDetected 2 EQ opportunities for Artist — Title:\n  - filter_sweep\n  - high_swap\n```\n\nOr (when disabled):\n```\nDJ EQ Automation: DISABLED\n```\n\n### In Code\n\n```python\nfrom autodj.render.eq_automation import EQAutomationDetector\n\ndetector = EQAutomationDetector(enabled=True)\nopportunities = detector.detect_for_track(...)\n\nfor opp in opportunities:\n    print(f\"{opp.cut_type.value}: bar {opp.bar}, conf {opp.confidence:.2f}\")\n```\n\n---\n\n## Documentation Files\n\n- **`/IMPLEMENTATION_SUMMARY.md`** - Detailed what was built\n- **`/DJ_EQ_AUTOMATION.md`** - How to use the feature\n- **`/DJ_EQ_RESEARCH.md`** - Why we designed it this way\n- **`/test_eq_automation.py`** - Working code examples\n\n---\n\n## Next Phase (Liquidsoap Integration)\n\nCurrent implementation:\n- ✅ **Detects** where EQ should be applied\n- ✅ **Generates** timeline with bar positions & parameters\n- ✅ **Exports** JSON for analysis\n- ⏳ **Not yet:** Actually applies filters in Liquidsoap\n\nPhase 2 will take detected opportunities and apply them as Liquidsoap DSP chain.\n\n---\n\n## Troubleshooting\n\n### \"EQ_ENABLED variable not found\"\nEnsure you're using the latest Makefile. It should auto-set `EQ_ENABLED` based on `EQ=` parameter.\n\n### \"No opportunities detected\"\nThis is normal if:\n- Audio features have low confidence values (<0.85)\n- Track has unusual structure\n\nCheck with:\n```bash\nEQ_ENABLED=true python3 -c \"from autodj.render.eq_automation import EQAutomationEngine; engine = EQAutomationEngine(128); print(engine.detect_opportunities({'intro_confidence': 0.92, 'num_bars': 32}))\"\n```\n\n### Tests fail\nRerun with debug output:\n```bash\npython3 -v test_eq_automation.py\n```\n\n---\n\n## Quick Reference\n\n| Command | Effect |\n|---------|--------|\n| `make quick-mix SEED=X TRACK_COUNT=3` | Generate with EQ (default) |\n| `make quick-mix SEED=X TRACK_COUNT=3 EQ=off` | Generate without EQ |\n| `make render` | Full render with EQ |\n| `make render EQ=off` | Full render without EQ |\n| `python3 test_eq_automation.py` | Run unit tests |\n| `EQ_ENABLED=true make render` | Explicit enable |\n| `EQ_ENABLED=false make render` | Explicit disable |\n\n---\n\n## Success Criteria\n\n✅ Implementation is successful if:\n1. Tests pass (7/7)\n2. EQ version sounds smoother than baseline\n3. No errors in logs\n4. Output files are identical in length (same content, just different EQ)\n5. Can toggle EQ on/off via CLI\n\n---\n\nReady to test! 🎧\n